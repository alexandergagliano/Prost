# .readthedocs.yml
version: 2

build:
  os: ubuntu-22.04   
  tools:
    python: "3.11"   

# Build documentation with Sphinx
sphinx:
  configuration: docs/conf.py  

# Python dependencies
python:
  install:
    - requirements: docs/requirements.txt  
    - method: pip
      path: .  
